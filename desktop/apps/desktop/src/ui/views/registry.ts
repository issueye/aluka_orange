/**
 * 视图注册表（迁移至 shell/registry.ts，此文件保留兼容再导出）
 */
export {
  SHELL_VIEWS,
  registerRuntimeView,
  clearRuntimeViews,
  pluginIcon,
  menuViews,
  viewLabel,
  type ShellViewDef,
} from "../shell/registry.ts";
